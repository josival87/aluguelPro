<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#0757e8"><title>Entrar — AlugaPro</title><link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml"><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head>
<body><div class="auth-page"><section class="auth-visual"><div><span class="brand-mark">A</span><h1>Seu imóvel.<br>Sua rotina.<br>Mais simples.</h1><p>Contratos, pagamentos e energia solar em um único lugar, com a segurança que você precisa.</p></div></section><section class="auth-form"><div><a class="brand" href="{{ route('properties.index') }}"><span class="brand-mark">A</span>Aluga<strong>Pro</strong></a><h1>Bem-vindo de volta</h1><p style="color:var(--muted);margin-bottom:28px">Entre para acessar sua área.</p>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<form method="post" action="{{ route('login.store') }}" class="stack">@csrf<div class="field"><label for="login">CPF do cliente ou login da equipe</label><input id="login" name="login" value="{{ old('login') }}" required autofocus autocomplete="username" inputmode="text" placeholder="CPF ou login"></div><div class="field"><label for="password">Senha</label><input id="password" type="password" name="password" required autocomplete="current-password"></div><label class="check-pill" style="border:0;padding-left:0"><input type="checkbox" name="remember" value="1"> Manter conectado</label><button class="btn btn-block" type="submit">Entrar <x-icon name="arrow"/></button></form><p style="margin-top:25px;text-align:center;color:var(--muted)">Ainda não é cliente? <a style="color:var(--blue);font-weight:700" href="{{ route('properties.index') }}">Encontre seu imóvel</a></p></div></section></div></body></html>
