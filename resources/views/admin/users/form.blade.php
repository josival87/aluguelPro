@extends('layouts.base', ['area'=>'admin'])
@section('title',($user->exists?'Editar':'Novo').' usuário — AlugaPro')
@section('content')
<div class="page-head"><div><h1>{{ $user->exists?'Editar usuário':'Novo usuário' }}</h1><p>Controle o acesso da equipe.</p></div></div><form class="card form-card" method="post" action="{{ $user->exists?route('admin.users.update',$user):route('admin.users.store') }}">@csrf @if($user->exists)@method('PUT')@endif<div class="form-grid"><div class="field"><label>Nome</label><input name="name" value="{{ old('name',$user->name) }}" required></div><div class="field"><label>CPF</label><input name="cpf" value="{{ old('cpf',$user->cpf) }}"></div><div class="field"><label>E-mail</label><input type="email" name="email" value="{{ old('email',$user->email) }}" required></div><div class="field"><label>WhatsApp</label><input name="phone" value="{{ old('phone',$user->phone) }}"></div><div class="field"><label>Login</label><input name="login" value="{{ old('login',$user->login) }}" required></div><div class="field"><label>Nível</label><select name="role"><option value="admin" @selected(old('role',$user->role)==='admin')>Administrador</option><option value="manager" @selected(old('role',$user->role)==='manager')>Gerente</option></select></div><div class="field"><label>Senha {{ $user->exists?'(deixe em branco para manter)':'' }}</label><input type="password" name="password" {{ $user->exists?'':'required' }}></div><div class="field"><label>Confirmar senha</label><input type="password" name="password_confirmation" {{ $user->exists?'':'required' }}></div><div class="field span-2"><label class="check-pill"><input type="checkbox" name="active" value="1" @checked(old('active',$user->exists?$user->active:true))> Usuário ativo</label></div></div><div class="form-actions"><a class="btn btn-ghost" href="{{ route('admin.users.index') }}">Cancelar</a><button class="btn">Salvar usuário</button></div></form>
@endsection
