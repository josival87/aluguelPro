@extends('layouts.base', ['area'=>'admin'])
@section('title','Características — AlugaPro')
@section('content')
<div class="page-head"><div><h1>Características</h1><p>Itens que ajudam o cliente a encontrar o imóvel certo.</p></div></div><div class="grid-2"><form class="card" method="post" action="{{ route('admin.features.store') }}">@csrf<h2>Nova característica</h2><div class="field"><label>Nome</label><input name="name" required placeholder="Ex.: Piscina"></div><button class="btn" style="margin-top:14px">Adicionar</button></form><section class="card"><h2>Cadastradas</h2>@forelse($features as $feature)<div class="list-row"><form class="list-row-main actions" method="post" action="{{ route('admin.features.update',$feature) }}">@csrf @method('PUT')<input class="input" name="name" value="{{ $feature->name }}" required><button class="icon-btn" aria-label="Salvar"><x-icon name="check" size="17"/></button></form><small>{{ $feature->properties_count }} imóvel(is)</small><form method="post" action="{{ route('admin.features.destroy',$feature) }}" data-confirm="Excluir esta característica?">@csrf @method('DELETE')<button class="icon-btn"><x-icon name="trash" size="17"/></button></form></div>@empty<div class="empty">Nenhuma característica.</div>@endforelse<div class="pagination">{{ $features->links() }}</div></section></div>
@endsection
