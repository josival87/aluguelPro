@extends('layouts.base', ['area'=>'admin'])
@section('title',$client->name.' — AlugaPro')
@section('content')
<div class="page-head"><div><a style="color:var(--blue);font-weight:700" href="{{ route('admin.clients.index') }}">← Clientes</a><h1 style="margin-top:8px">{{ $client->name }}</h1><p>{{ $client->cpf }} · {{ $client->phone }}</p></div><div class="head-actions"><x-status :value="$client->status"/><a class="btn btn-outline" href="{{ route('admin.clients.edit',$client) }}"><x-icon name="edit"/> Editar</a></div></div><div class="metrics"><div class="card metric"><div class="metric-top"><span>Em aberto</span><span class="metric-icon"><x-icon name="calendar"/></span></div><strong>R$ {{ number_format((float)$summary['open'],2,',','.') }}</strong></div><div class="card metric"><div class="metric-top"><span>Total pago</span><span class="metric-icon"><x-icon name="money"/></span></div><strong>R$ {{ number_format((float)$summary['paid'],2,',','.') }}</strong></div><div class="card metric"><div class="metric-top"><span>Contratos</span><span class="metric-icon"><x-icon name="file"/></span></div><strong>{{ $client->leases->count() }}</strong></div><div class="card metric"><div class="metric-top"><span>Documentos</span><span class="metric-icon"><x-icon name="file"/></span></div><strong>{{ $client->documents->count() }}</strong></div></div>
<section class="card"><h2>Histórico de aluguéis</h2>@forelse($client->leases as $lease)<a class="list-row" href="{{ route('admin.leases.show',$lease) }}"><span class="metric-icon"><x-icon name="home"/></span><span class="list-row-main"><strong>{{ $lease->property->title }}</strong><small>{{ $lease->start_date?->format('d/m/Y') ?? 'Início pendente' }} — {{ $lease->end_date?->format('d/m/Y') ?? 'Fim pendente' }}</small></span><span><small style="display:block;color:var(--muted)">Em aberto</small><strong>R$ {{ number_format((float)$lease->charges->where('status','open')->sum('amount'),2,',','.') }}</strong></span><span><small style="display:block;color:var(--muted)">Pago</small><strong>R$ {{ number_format((float)$lease->charges->where('status','paid')->sum('amount'),2,',','.') }}</strong></span><x-status :value="$lease->status"/></a>@empty<div class="empty">Cliente sem aluguéis.</div>@endforelse</section>
@endsection
